Определение подмножества (IsSubset)
Необходимо реализовать метод IsSubset, который выдает булево значение, которое показывает, включает ли переданное множество otherSet все элементы текущего множества:

// IsSubset - проверяет, является ли текущее множество подмножеством otherSet
func (s *SetOnSlice[T]) IsSubset(otherSet *SetOnSlice[T]) bool {
	panic("реализуйте данный метод")
}