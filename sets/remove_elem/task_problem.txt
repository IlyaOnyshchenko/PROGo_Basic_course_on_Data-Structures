Удаление элемента (Remove)
Необходимо реализовать метод Remove, который будет возвращать true, в случае удаления, иначе false:

// Remove - удаляет значение из множества
func (s SetOnMap[T]) Remove(item T) bool {
    panic("реализуйте данный метод")
}