Определение подмножества (IsSubset)
Необходимо реализовать метод IsSubset:

// IsSubset - проверяет, является ли otherSet подмножеством текущего множества
func (s SetOnMap[T]) IsSubset(otherSet SetOnMap[T]) bool {
    panic("реализуйте данный метод")
}
                  
Необходимо проверить все ли значения из otherSet есть в s.

Sample Input:

1 2 3
3

Sample Output:

YES