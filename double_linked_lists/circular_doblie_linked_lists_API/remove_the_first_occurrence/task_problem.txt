Удаление первого вхождения
Необходимо реализовать метод Remove, который удаляет первое вхождение указанного элемента из списка. Данный метод возвращает значение true, если элемент item успешно удален, в противном случае — значение false.  Метод также возвращает false, если элемент item не найден в списке:

// Remove - удаление первого найденного узла по значению
func (list *DoubleCircularLinkedList) Remove(item int) bool {
    panic("реализуйте данный метод")
}